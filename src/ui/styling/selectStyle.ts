/**
 * Select component theme configuration
 * Uses the standard theme factory for consistency
 */

import { createSelectTheme } from "@/styling/themeUtils";

/**
 * Default select theme with standard styling
 * Provides consistent appearance for select/dropdown components
 */
export const selectStyle = createSelectTheme();
